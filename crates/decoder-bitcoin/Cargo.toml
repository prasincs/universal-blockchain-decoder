[package]
name = "decoder-bitcoin"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Bitcoin transaction decoder for universal-decoder"

[dependencies]
universal-decoder-core = { path = "../universal-decoder-core" }
serde = { workspace = true }
thiserror = { workspace = true }
# hex - Use vendored version via: use universal_decoder_core::hex;
# NOTE: bitcoin crate is temporarily in dependencies for Phase 1.5
# Will be moved to dev-dependencies in Phase 2 when pure Rust decoder is implemented
bitcoin = "0.31"

[dev-dependencies]
# Blockchain libraries for test validation ONLY
# Production code will use pure Rust parsing (Phase 2)
serde_json = { workspace = true }
proptest = { workspace = true }
