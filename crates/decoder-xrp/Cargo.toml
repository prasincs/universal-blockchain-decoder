[package]
name = "decoder-xrp"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "XRP Ledger transaction decoder for universal-decoder"

[dependencies]
universal-decoder-core = { path = "../universal-decoder-core" }
decoder-primitives = { path = "../decoder-primitives" }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
# No standard Rust library for XRP binary codec
# Will need custom validation implementation
serde_json = { workspace = true }
proptest = { workspace = true }
