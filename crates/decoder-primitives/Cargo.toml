[package]
name = "decoder-primitives"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Low-level parsing primitives for blockchain transaction decoders"

[dependencies]
# ZERO external dependencies - only universal-decoder-core for error types
universal-decoder-core = { path = "../universal-decoder-core" }

[dev-dependencies]
# Testing only
proptest = { workspace = true }
